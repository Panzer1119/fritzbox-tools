[project]
name = "fritz-log-agent"
version = "0.1.0"
description = "Download Fritz!Box logs and stream them to stdout."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[project.scripts]
fritz-log-agent = "fritz_log_agent.cli:main"
